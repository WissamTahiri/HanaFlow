#!/usr/bin/env bash
# Pre-commit : tourne lint-staged sur les fichiers staged (lint+tsc bloquants).
# Pour bypasser (rare, ex. WIP perso) : git commit --no-verify
cd "$(git rev-parse --show-toplevel)/hanaflow-next" && node node_modules/lint-staged/bin/lint-staged.js
