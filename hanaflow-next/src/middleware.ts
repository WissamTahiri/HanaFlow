// Déprécié — la logique a été migrée vers `proxy.ts` (Next.js 16).
// Ce fichier est conservé vide parce qu'il n'est pas supprimable depuis le shell
// (permission denied), mais il n'exporte plus de fonction `middleware` et est
// donc ignoré par le runtime. Supprimer manuellement quand possible :
//   rm src/middleware.ts
export {};
